{
  "summarize_context": [
    {
      "prompt": "Summarize the following long-term memory into concise actionable context while preserving goals, constraints, and pending tasks.",
      "instructions": [
        "Keep output short and actionable.",
        "Preserve goals, constraints, blockers, and next steps.",
        "Do not invent details that are not in the input."
      ],
      "models": ["gemma4:e2b", "gemma3:4b"]
    }
  ],
  "describe_screenshot": [
    {
      "prompt": "Describe the current desktop screenshot. Include active app/window, visible UI elements, key text, warnings/dialogs, and interactable UI targets.",
      "instructions": [
        "Prioritize details that influence the next action.",
        "Mention coordinates only when clearly inferable.",
        "Separate confirmed observations from uncertain guesses."
      ],
      "models": ["gemma4:e2b", "gemma3:4b"]
    }
  ],
  "verify_change_requires_thinking": [
    {
      "prompt": "Given previous and current screenshots, answer if these two screenshots are about the same UI state. Return only JSON with keys: same_state (bool), reason (string).",
      "instructions": [
        "Focus on UI state changes, not tiny visual noise.",
        "Ignore loading indicators and other temporary UI elements.",
        "Treat dialogs, and navigation changes as meaningful.",
        "Return strict JSON only."
      ],
      "models": ["gemma4:e2b", "gemma3:4b"]
    }
  ],
  "brain_decide_action": [
    {
      "prompt": "Given task objective, screenshot context, and available tools, decide the best next sequence of tool calls.",
      "instructions": [
        "Choose the best next sequence of tool calls that advances the task objective.",
        "Create detailed tool instructions for each tool call."
      ],
      "models": ["gemma4:e2b", "gemma3:4b"]
    }
  ],
  "brain_verify_script_step": [
    {
      "prompt": "You are verifying whether the current scripted task step is satisfied in the screenshot. You will see the full numbered script and which step is current.",
      "instructions": [
        "Decide if the current step goal is actually accomplished on screen based on visible UI and text.",
        "Return strict JSON only (no markdown), single object with keys: accomplished (bool), branch (string), target_step (number or null), reason (string).",
        "branch must be one of: advance, retry, skip, goto.",
        "Use branch advance only when accomplished is true (move to next script line).",
        "When accomplished is false: use retry to repeat the same step, skip to abandon this line and move to the next, or goto to jump to a specific script line (use target_step as the 1-based line number from the numbered list).",
        "For goto, target_step must be the line number shown before each script line (1 to N). Set target_step to null for other branches.",
        "Do not invent UI elements; base conclusions on the image and script text only."
      ],
      "models": ["gemma4:e2b", "gemma3:4b"]
    }
  ],
  "classify_interruption": [
    {
      "prompt": "We are doing a task. Determine whether the newly arrived screenshot interrupts the first screenshot during the task. Return strict JSON with interruption (bool), replace_state (bool), and reason.",
      "instructions": [
        "Classify interruption based on task impact, not only visual difference.",
        "Set replace_state true only when old state is obsolete.",
        "Return strict JSON only."
      ],
      "models": ["gemma4:e2b", "gemma3:4b"]
    }
  ]
}
